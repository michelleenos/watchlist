[project]
name = "api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiofiles>=25.1.0",
    "fastapi[standard]>=0.138.0",
    "httpx>=0.28.1",
    "nanoid>=2.0.0",
    "pillow>=12.2.0",
	"psycopg[binary,pool]>=3.3.4",
]

[project.scripts]
migrate = "migrations.migrate:main"